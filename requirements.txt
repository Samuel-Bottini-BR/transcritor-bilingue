# Dependencias do Transcritor Bilingue - versoes travadas
#
# Este arquivo descreve o ambiente da MAQUINA DE DESENVOLVIMENTO (onde o
# app roda pelo Python e onde o instalador e gerado). O segundo PC nao
# instala nada disto: ele recebe o instalador .exe pronto.
#
# Instalar:
#   py -3.14 -m venv .venv
#   .\.venv\Scripts\python -m pip install -U pip
#   .\.venv\Scripts\python -m pip install -r requirements.txt

# --- nucleo do app ---
faster-whisper==1.2.1
ctranslate2==4.8.2
PySide6==6.11.2
yt-dlp==2026.8.19
av==18.1.0
huggingface_hub==1.30.0
tokenizers==0.23.2
tqdm==4.70.0

# --- GPU NVIDIA (so faz efeito em PC com placa NVIDIA) ---
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12==9.25.1.1
nvidia-cuda-runtime-cu12==12.9.79

# --- empacotamento (so na maquina que gera o instalador) ---
pyinstaller==6.22.2

# --- ferramenta de teste (gerar_audio_teste.py) ---
edge-tts==7.2.8
soundfile==0.14.0
numpy==2.5.3
